import { ObservedDirective } from './observed.directive';

describe('ObservedDirective', () => {
  it('should create an instance', () => {
    const directive = new ObservedDirective();
    expect(directive).toBeTruthy();
  });
});
